# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/goforge
/server
/cmd/server/server

# Build output
/bin/
/dist/

# Test
*.test
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# Dependency directory (use go mod vendor if needed)
/vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
# Local MCP server config — contains tokens / internal endpoints; never commit.
.mcp.json
mcp.json

# SQLite (checkpoint store runtime data)
*.db
*.db-journal
*.db-wal
*.db-shm

# Runtime logs (e.g. goforge.log from `goforge serve`/`run`) — may contain
# transcripts / internal endpoints; never commit.
*.log

# Cursor / agent artifacts (local only)
.cursor/
session-handoff.md
progress.md

# Sensitive words list (local only, never commit)
.sensitive-words

# Plan files
*.plan.md

# Claude Code local overrides (project hooks/settings committed, local overrides not)
.claude/settings.local.json
